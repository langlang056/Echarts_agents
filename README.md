# 📊 LocalInsight - 智能本地数据分析师

> 零代码 · 隐私安全 · 交互式可视化

LocalInsight 是一个基于 AgentScope 多智能体框架的本地数据分析工具。用户只需上传 CSV/Excel 文件并提问，系统即可自动清洗数据、生成可交互的 ECharts 图表，并提供商业维度的文字分析报告。

## ✨ 核心特点

- 🔒 **完全本地化**: 数据处理完全在本地进行，除 LLM API 调用外，数据不离境
- 💬 **对话式交互**: 使用自然语言提问，无需编写代码
- 🎨 **交互式图表**: 基于 Pyecharts 的动态 ECharts 可视化，支持缩放、筛选等交互
- 🤖 **双智能体协作**:
  - **数据工程师 Agent**: 处理数据并生成可视化
  - **商业分析师 Agent**: 提供通俗易懂的商业洞察
- 🛡️ **安全可控**: 内置代码安全检查，防止危险操作
- 🔧 **自动修复**: 基于 ReAct 机制，自动处理错误并重试

## 🏗️ 技术架构

```
┌─────────────────────────────────────────────────┐
│              Streamlit Web UI                   │
│         (文件上传 + 聊天界面 + 图表渲染)          │
└────────────────┬────────────────────────────────┘
                 │
                 ↓
┌─────────────────────────────────────────────────┐
│          AgentScope Orchestrator                │
│                                                 │
│  ┌─────────────────────────────────────────┐   │
│  │  Data Engineer Agent (ReActAgent)       │   │
│  │  ├─ read_data_schema()                  │   │
│  │  ├─ execute_python_safe()               │   │
│  │  └─ validate_html_output()              │   │
│  └──────────────┬──────────────────────────┘   │
│                 │ execution_log                 │
│                 ↓                                │
│  ┌─────────────────────────────────────────┐   │
│  │  Business Analyst Agent (AgentBase)     │   │
│  │  └─ generate_business_insights()        │   │
│  └─────────────────────────────────────────┘   │
└─────────────────────────────────────────────────┘
                 │
                 ↓
┌─────────────────────────────────────────────────┐
│    LLM Providers (DashScope / OpenAI)           │
└─────────────────────────────────────────────────┘
```

### 技术栈

- **多智能体框架**: [AgentScope](https://github.com/agentscope-ai/agentscope)
- **大模型**: DashScope (Qwen) / OpenAI (GPT)
- **Web 界面**: Streamlit
- **数据可视化**: Pyecharts (ECharts)
- **数据处理**: Pandas, NumPy

## 🚀 快速开始

### 1. 环境要求

- Python 3.10 或更高版本
- 推荐使用虚拟环境

### 2. 安装依赖

```bash
# 克隆或下载项目
cd echarts_agent

# 创建虚拟环境（推荐）
python -m venv venv

# 激活虚拟环境
# Windows:
venv\Scripts\activate
# macOS/Linux:
source venv/bin/activate

# 安装依赖
pip install -r requirements.txt
```

### 3. 配置 API Key

创建 `.env` 文件（基于 `.env.example`）：

```bash
cp .env.example .env
```

编辑 `.env` 文件，填入你的 API Key：

```env
# 使用 DashScope (通义千问)
DASHSCOPE_API_KEY=your_dashscope_api_key_here

# 或使用 OpenAI
OPENAI_API_KEY=your_openai_api_key_here
```

**获取 API Key:**
- **DashScope**: https://dashscope.console.aliyun.com/
- **OpenAI**: https://platform.openai.com/api-keys

### 4. 运行应用

```bash
streamlit run app.py
```

应用将在浏览器中自动打开（默认地址: http://localhost:8501）

## 📖 使用指南

### 基本流程

1. **配置模型**
   - 在左侧边栏选择模型提供商（DashScope 或 OpenAI）
   - 输入 API Key（或在 `.env` 中配置）
   - 选择模型版本（推荐：qwen-max 或 gpt-4）

2. **初始化智能体**
   - 点击"🚀 初始化智能体"按钮
   - 等待智能体初始化完成

3. **上传数据文件**
   - 支持 CSV、XLSX、XLS 格式
   - 文件在本地处理，不会上传到云端

4. **提出问题**
   - 在聊天框中输入你的问题
   - AI 会自动生成可视化图表和分析报告

### 示例问题

#### 趋势分析
```
- "分析2024年各季度的销售趋势"
- "展示每月收入的变化情况"
- "画出过去12个月的用户增长曲线"
```

#### 对比分析
```
- "对比不同产品类别的销售额"
- "各个地区的销售业绩对比"
- "比较不同渠道的转化率"
```

#### 排名分析
```
- "找出销售额最高的前10个产品"
- "显示表现最好的5个销售团队"
- "哪些客户的消费金额最高？"
```

#### 分布分析
```
- "分析客户年龄分布"
- "展示订单金额的分布情况"
- "不同价格区间的产品数量"
```

## 📁 项目结构

```
echarts_agent/
├── app.py                          # Streamlit 主应用
├── agents.py                       # Agent 定义和配置
├── tools.py                        # 自定义工具函数
├── requirements.txt                # Python 依赖
├── .env.example                    # 环境变量模板
├── .env                           # 环境变量（需自行创建）
├── README.md                       # 项目说明
├── docs/
│   └── agentscope_api_reference.md # AgentScope API 文档
└── temp/                           # 临时文件目录（自动创建）
    ├── data.csv                    # 用户上传的数据
    └── visual_result.html          # 生成的可视化
```

## 🔧 核心组件说明

### 1. Data Engineer Agent (数据工程师)

**类型**: ReActAgent（支持推理和行动循环）

**职责**:
- 使用 `read_data_schema` 理解数据结构
- 编写 Python 代码处理数据
- 生成 Pyecharts 可视化图表
- 输出关键统计指标

**可用工具**:
- `read_data_schema(file_path)`: 读取数据结构和前5行
- `execute_python_safe(code)`: 安全执行 Python 代码
- `validate_html_output(file_path)`: 验证可视化文件生成

**强制约束**:
- 必须将 Pandas Series 转换为 Python list（使用 `.tolist()`）
- 必须保存图表为 `./temp/visual_result.html`
- 必须用 `print()` 输出关键指标
- 禁止使用 `plt.show()`、`os.system`、网络访问

### 2. Business Analyst Agent (商业分析师)

**类型**: AgentBase（无工具，纯对话）

**职责**:
- 读取数据工程师的执行日志
- 生成通俗易懂的商业分析报告
- 提供可操作的建议和洞察

**输出格式**:
- 核心发现（2-3 句总结）
- 详细分析（趋势、对比、异常）
- 风险提示
- 可操作建议

### 3. 自定义工具 (tools.py)

所有工具函数都返回 `ToolResponse` 对象，并包含详细的 docstring 供 LLM 理解。

#### `read_data_schema(file_path: str) -> ToolResponse`
读取数据文件的结构信息，返回列名、数据类型、前5行样本。

#### `execute_python_safe(code: str, working_dir: str) -> ToolResponse`
安全执行 Python 代码，包含以下特性：
- 安全检查（阻止 `os.system`, `subprocess` 等危险操作）
- 输出捕获（stdout 和 stderr）
- 受限的全局命名空间
- 自动导入 pandas、pyecharts 等常用库

#### `validate_html_output(file_path: str) -> ToolResponse`
验证可视化文件是否成功生成，检查文件存在性、大小、HTML 结构。

## ⚠️ 常见问题

### 1. 安装 AgentScope 失败

```bash
# 如果 pip 安装失败，尝试从源码安装
git clone https://github.com/agentscope-ai/agentscope.git
cd agentscope
pip install -e .
```

### 2. Pyecharts 图表显示为空

**原因**: 数据类型不兼容（Pandas Series 无法序列化为 JSON）

**解决方案**:
```python
# ❌ 错误
bar.add_yaxis("销售额", df['sales'])

# ✅ 正确
bar.add_yaxis("销售额", df['sales'].tolist())
```

### 3. API Key 配置无效

确保：
- `.env` 文件在项目根目录
- API Key 格式正确（无引号、无空格）
- 重启 Streamlit 应用以加载新配置

### 4. 执行代码时出现权限错误

**解决方案**: 检查 `./temp` 目录是否有写入权限

```bash
# 创建目录并设置权限
mkdir temp
chmod 755 temp
```

### 5. 模型调用超时

**原因**: 网络问题或模型负载高

**解决方案**:
- 检查网络连接
- 尝试更换模型（如 qwen-turbo 或 gpt-3.5-turbo）
- 减少数据量

## 🔐 安全性说明

### 代码执行安全

`execute_python_safe` 工具包含以下安全机制：

1. **正则表达式检查**: 阻止危险操作关键字
   - `os.system`, `subprocess`
   - `eval`, `exec`, `__import__`
   - 文件删除操作

2. **受限命名空间**: 仅提供必要的内置函数和库

3. **工作目录隔离**: 代码在 `./temp` 目录执行

**注意**: 此工具用于教育和原型开发，**不应在生产环境中执行不受信任的代码**。

### 数据隐私

- 所有数据处理在本地完成
- 仅以下信息会发送到 LLM API:
  - 用户问题文本
  - 数据结构信息（列名、类型、前5行样本）
  - 代码执行日志（print 输出）
- 完整数据集**不会**上传到云端

## 🛠️ 开发指南

### 添加新工具

1. 在 `tools.py` 中定义工具函数：

```python
from agentscope.service import ToolResponse

def my_new_tool(param: str) -> ToolResponse:
    """Tool description for LLM

    Args:
        param (str): Parameter description

    Returns:
        ToolResponse: Result description
    """
    try:
        # Your logic here
        result = process(param)
        return ToolResponse(
            content=result,
            is_success=True
        )
    except Exception as e:
        return ToolResponse(
            content=f"Error: {str(e)}",
            is_success=False
        )
```

2. 在 `agents.py` 中注册工具：

```python
toolkit.register_tool_function(my_new_tool)
```

### 自定义 Agent Prompt

编辑 `agents.py` 中的 `DATA_ENGINEER_PROMPT` 或 `BUSINESS_ANALYST_PROMPT` 常量，根据你的需求调整系统指令。

### 扩展可视化类型

在 `tools.py` 的 `execute_python_safe` 函数中，添加更多 Pyecharts 图表类型：

```python
from pyecharts.charts import Radar, Funnel, Gauge  # 新增图表类型

exec_globals['Radar'] = Radar
exec_globals['Funnel'] = Funnel
exec_globals['Gauge'] = Gauge
```

## 📊 支持的图表类型

LocalInsight 支持所有 Pyecharts 图表类型，包括但不限于：

| 图表类型 | 适用场景 |
|---------|---------|
| **Bar** (柱状图) | 类别对比、排名 |
| **Line** (折线图) | 趋势分析、时间序列 |
| **Pie** (饼图) | 占比分析、分布 |
| **Scatter** (散点图) | 相关性分析、分布 |
| **Boxplot** (箱线图) | 数据分布、异常检测 |
| **HeatMap** (热力图) | 矩阵数据、相关性 |
| **Radar** (雷达图) | 多维度对比 |
| **Funnel** (漏斗图) | 转化率分析 |
| **Gauge** (仪表盘) | 单一指标展示 |
| **Graph** (关系图) | 网络关系、拓扑 |

更多图表类型请参考 [Pyecharts 官方文档](https://pyecharts.org/#/zh-cn/intro)

## 🤝 贡献指南

欢迎提交 Issue 和 Pull Request！

1. Fork 本项目
2. 创建特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 开启 Pull Request

## 📄 许可证

本项目仅用于学习和研究目的。

## 🙏 致谢

- [AgentScope](https://github.com/agentscope-ai/agentscope) - 优秀的多智能体框架
- [Pyecharts](https://github.com/pyecharts/pyecharts) - 强大的 ECharts Python 封装
- [Streamlit](https://streamlit.io/) - 简洁优雅的 Web 框架

## 📮 联系方式

如有问题或建议，欢迎通过以下方式联系：

- 提交 GitHub Issue
- 邮件: [您的邮箱]

---

**⚡ Happy Analyzing!**
